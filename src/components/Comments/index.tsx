import { OneComment } from "./OneComment";

export function Comments({ comments }: { comments: any }) {
  return (
    <div>
      {/* {comments.map((item) => {
        return(
          <OneComment />
        )
      })} */}
    </div>
  );
}
